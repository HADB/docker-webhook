## What's Changed
{% for type_, commits in release["elements"] | dictsort %}
### {{ type_ | capitalize }}
{%- if type_ != "unknown" %}
{% for commit in commits %}
* {{ commit.descriptions[0] }} ([`{{ commit.short_hash }}`]({{ commit.hexsha | commit_hash_url }}))
{%- endfor %}{% endif %}{% endfor %}

{%- if context is defined and context.history.released | length > 0 -%}
{% from ".macros.j2" import version_compare_url %}
{% from ".macros.j2" import changelog_file_url %}

{% set releases = context.history.released.items() | list %}
{% set prev_version = releases[1][0].as_tag() %}

**Full changes**: {{ prev_version | compare_url(version.as_tag()) }}

**All changelog**: [CHANGELOG.md](https://github.com/{{ context.repo_owner }}/{{ context.repo_name }}/blob/{{ context.branch_name }}/CHANGELOG.md)
{%- endif %}
